name: censor_it
description: A Flutter library for censoring text based on predefined patterns and customizable characters.
version: 1.1.1
repository: https://github.com/pavluke/censor_it

environment:
  sdk: ^3.3.4

dev_dependencies:
  lints: ^3.0.0
